/* eslint-disable no-unused-vars */
import React from "react";

const Skill = ({ ...props }) => {
  return (
    <>
      <section id={props.id}>
        <div>Skill</div>
      </section>
    </>
  );
};

export default Skill;
