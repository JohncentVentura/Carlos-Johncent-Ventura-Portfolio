/* eslint-disable no-unused-vars */
import React from "react";

import { Div } from "../App";

const Skill = ({ ...props }) => {
  return (
    <>
      <section id={props.id}>
        <Div>Skill</Div>
      </section>
    </>
  );
};

export default Skill;
