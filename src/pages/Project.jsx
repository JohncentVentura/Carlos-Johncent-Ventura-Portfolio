/* eslint-disable no-unused-vars */
import React from "react";

const Project = ({ ...props }) => {
  return (
    <>
      <section id={props.id}>
        <div>Project</div>
      </section>
    </>
  );
};

export default Project;
