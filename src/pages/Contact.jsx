/* eslint-disable no-unused-vars */
import React from "react";

const Contact = ({ ...props }) => {
  return (
    <>
      <section id={props.id}>
        <div>Contact</div>
      </section>
    </>
  );
};

export default Contact;
