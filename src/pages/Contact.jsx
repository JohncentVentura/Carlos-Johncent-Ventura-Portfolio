/* eslint-disable no-unused-vars */
import React from "react";

import { Div } from "../App";

const Contact = ({ ...props }) => {
  return (
    <>
      <section id={props.id}>
        <Div>Contact</Div>
      </section>
    </>
  );
};

export default Contact;
