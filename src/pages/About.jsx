/* eslint-disable no-unused-vars */
import React from "react";

import { Div } from "../App";

const About = ({ ...props }) => {
  return (
    <>
      <section id={props.id}>
        <Div>About</Div>
      </section>
    </>
  );
};

export default About;
